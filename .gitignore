node_modules/
.expo/
dist/
web-build/
*.log
firebase-debug.log
firestore-debug.log
ui-debug.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Local secrets — never commit the real Firebase config
.env
.env.local

# Native build output — regenerated by `npx expo prebuild`
ios/
android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
google-services.json
GoogleService-Info.plist

.DS_Store

# Output of `npm run bundle:check`
.expo-bundle-check/
dist/

# Scratch file the brand-asset script writes while pulling the JPEG out of a PDF.
.brand-source.jpg
