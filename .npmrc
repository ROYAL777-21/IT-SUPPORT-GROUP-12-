# expo-router bundles web-only dependencies (radix, vaul) that peer-depend on
# react-dom. This project has no web target, so react-dom is deliberately not
# installed and npm's strict peer resolution has nothing to satisfy them with.
# Expo's own tooling assumes this looser resolution.
legacy-peer-deps=true
